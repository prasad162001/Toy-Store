STORAGE_PROVIDER=local
CLOUDINARY_URL=cloudinary://<api_key>:<api_secret>@<cloud_name>

# Required when STORAGE_PROVIDER=local and the frontend uses a separate origin.
PUBLIC_API_URL=http://localhost:4000